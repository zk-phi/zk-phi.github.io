---
title: "zk-phi の部屋 :: 最近の活動"
permalink: /activities/index.html
layout: layouts/common.njk
styles:
  - css/activities.css
---

<h1 id="title" class="block">
  <span class="rot">💻</span> 最近の活動 <span class="rot">💻</span>
</h1>

<p class="block">
  直近１年の間に公開したフリーソフト・文書などをまとめました。
</p>

<p class="block">
  <a class="link" href="/">&lt; 戻る</a>
</p>

<hr />

{% for m in recentActivities %}
<div class="left block">
  <h2 class="header">
    <span class="puni">{{ monthEmoji[m.month] }}</span>
    {{ m.month + 1 }}月<small>/{{ m.year }}</small>
  </h2>
  <ul class="list">
    {% for i in m.items %}
    <li class="item">
      <div class="activity-meta">
        <span class="activity-tag">{{ i.category | safe }}</span>
        <span class="activity-source">{{ i.source | safe }}</span>
      </div>
      <a class="link" target="_blank" rel="noreferrer" href="{{ i.link | safe }}">
        {{ i.title | safe }}
      </a>
    </li>
    {% endfor %}
  </ul>
</div>
{% endfor %}

<hr />

<p class="block">
  <a class="link" href="/">&lt; 戻る</a>
</p>
